# Phase 2: Backend API 重构

> 实施日期: 2026-02-09
> 版本: 2.0.0
> 状态: ✅ 已完成

根据 [UNIFIED_DYNAMIC_STRATEGY_ARCHITECTURE.md](../docs/UNIFIED_DYNAMIC_STRATEGY_ARCHITECTURE.md) Phase 2 设计，完成了Backend API的重构，实现了统一的策略管理系统。

## ✅ 完成的工作

### 1. Pydantic Schema 模型

**文件**: `backend/app/schemas/strategy.py`

创建了完整的统一策略系统 Schema：

- ✅ `StrategyCreate` - 创建策略请求模型
- ✅ `StrategyUpdate` - 更新策略请求模型
- ✅ `StrategyResponse` - 策略响应模型（完整信息）
- ✅ `StrategyListResponse` - 策略列表响应模型（不含代码）
- ✅ `StrategyStatistics` - 策略统计信息模型
- ✅ `ValidateCodeRequest` - 代码验证请求模型
- ✅ `ValidationResult` - 代码验证结果模型
- ✅ `SimplifiedBacktestRequest` - 简化的回测请求模型

**核心特性**:
- 统一的数据验证
- 支持三种来源类型：builtin/ai/custom
- 完整的字段验证和约束
- 清晰的文档和示例

---

### 2. 统一策略 Repository

**文件**: `backend/app/repositories/strategy_repository.py`

实现了完整的数据访问层：

#### 核心方法：

| 方法 | 功能 | 说明 |
|------|------|------|
| `create()` | 创建策略 | 自动计算code_hash，保存完整代码 |
| `get_by_id()` | 获取策略详情 | 包含完整代码 |
| `get_by_name()` | 按名称获取 | 支持唯一名称查询 |
| `list_all()` | 获取策略列表 | 支持筛选、分页、搜索 |
| `update()` | 更新策略 | 自动重新计算hash，增加版本号 |
| `delete()` | 删除策略 | 物理删除 |
| `increment_usage_count()` | 增加使用次数 | 更新last_used_at |
| `increment_backtest_count()` | 增加回测次数 | - |
| `update_backtest_metrics()` | 更新回测指标 | 计算移动平均 |
| `get_statistics()` | 获取统计信息 | 按来源/类别/验证状态/风险分组 |
| `check_name_exists()` | 检查名称是否存在 | 用于唯一性验证 |

**特性**:
- 支持复杂筛选（来源类型、类别、启用状态、验证状态、关键词搜索）
- 自动处理JSON字段（default_params、validation_errors等）
- 分页支持
- 统计分析

---

### 3. 统一策略 API 端点

**文件**: `backend/app/api/endpoints/strategies.py`

实现了完整的 RESTful API：

#### API 端点列表：

| 方法 | 路径 | 功能 | 状态 |
|------|------|------|------|
| GET | `/api/strategies/statistics` | 获取策略统计信息 | ✅ |
| POST | `/api/strategies/validate` | 验证策略代码 | ✅ |
| POST | `/api/strategies` | 创建策略 | ✅ |
| GET | `/api/strategies` | 获取策略列表 | ✅ |
| GET | `/api/strategies/{id}` | 获取策略详情 | ✅ |
| PUT | `/api/strategies/{id}` | 更新策略 | ✅ |
| DELETE | `/api/strategies/{id}` | 删除策略 | ✅ |
| GET | `/api/strategies/{id}/code` | 获取策略代码 | ✅ |
| POST | `/api/strategies/{id}/test` | 测试策略 | ✅ |

#### 核心特性：

1. **代码验证**
   - 使用 Core 的 `CodeSanitizer` 进行安全检查
   - AST 分析
   - 危险操作检测
   - 安全问题识别

2. **策略保护**
   - 内置策略（builtin）不允许修改代码
   - 内置策略不允许删除
   - 名称唯一性检查

3. **使用统计**
   - 自动记录使用次数
   - 更新最后使用时间
   - 计算平均回测指标

4. **错误处理**
   - 统一的异常处理
   - 详细的错误信息
   - 适当的HTTP状态码

---

### 4. 简化的回测 API

**文件**: `backend/app/api/endpoints/backtest.py`

新增了简化的回测端点：

#### 新端点：

```
POST /api/backtest/run-v3
```

**特性**:
- ✅ 只需要 `strategy_id` 参数
- ✅ 从 `strategies` 表统一加载策略
- ✅ 移除复杂的 `strategy_type` 参数
- ✅ 自动验证策略状态（启用状态、验证状态）
- ✅ 自动更新策略统计（回测次数、平均指标）
- ✅ 记录执行历史

**请求示例**:
```json
{
  "strategy_id": 1,
  "stock_pool": ["000001.SZ", "600000.SH"],
  "start_date": "2023-01-01",
  "end_date": "2023-12-31",
  "initial_capital": 1000000.0,
  "strict_mode": true
}
```

**注意**: 当前版本包含占位符代码，等待 Phase 1（统一 DynamicCodeLoader）完成后可完全实现。

---

### 5. 模块导出更新

#### `backend/app/schemas/__init__.py`
- ✅ 导出所有策略相关 Schema

#### `backend/app/repositories/__init__.py`
- ✅ 导出 `StrategyRepository`
- ✅ 标记旧 Repository 为将废弃

#### `backend/app/api/__init__.py`
- ✅ 注册统一策略路由: `/api/strategies`
- ✅ 标记旧路由为将废弃:
  - `/api/strategy-configs` (旧)
  - `/api/dynamic-strategies` (旧)

---

## 📊 API 对比

### 旧架构 vs 新架构

| 功能 | 旧架构 | 新架构 |
|------|--------|--------|
| **策略列表** | 3个端点（configs/dynamic/predefined） | 1个端点（/strategies） |
| **策略详情** | 分散在不同端点 | 统一端点（/strategies/{id}） |
| **代码可见性** | 预定义策略不可见 | 所有策略代码完全可见 |
| **回测选择** | 需指定 strategy_type | 只需 strategy_id |
| **数据表** | 2张表 | 1张表（strategies） |
| **筛选功能** | 基础筛选 | 高级筛选（来源/类别/状态/搜索） |

---

## ✅ 测试覆盖

### 单元测试

**文件**: `tests/unit/api/test_strategies_api.py`

创建了完整的API单元测试（14个测试用例）：

| 测试用例 | 功能 | 状态 |
|---------|------|------|
| `test_get_statistics` | 获取策略统计信息 | ✅ PASSED |
| `test_validate_code_success` | 验证代码 - 成功 | ✅ PASSED |
| `test_validate_code_failed` | 验证代码 - 失败 | ✅ PASSED |
| `test_list_strategies` | 获取策略列表 | ✅ PASSED |
| `test_get_strategy_detail` | 获取策略详情 | ✅ PASSED |
| `test_get_strategy_not_found` | 获取不存在的策略 | ✅ PASSED |
| `test_create_strategy_success` | 创建策略 - 成功 | ✅ PASSED |
| `test_create_strategy_duplicate_name` | 创建策略 - 重复名称 | ✅ PASSED |
| `test_update_strategy_success` | 更新策略 - 成功 | ✅ PASSED |
| `test_update_builtin_strategy_code_forbidden` | 更新内置策略代码 - 禁止 | ✅ PASSED |
| `test_delete_strategy_success` | 删除策略 - 成功 | ✅ PASSED |
| `test_delete_builtin_strategy_forbidden` | 删除内置策略 - 禁止 | ✅ PASSED |
| `test_get_strategy_code` | 获取策略代码 | ✅ PASSED |
| `test_test_strategy` | 测试策略 | ✅ PASSED |

**测试结果**: 14/14 passed (100% 通过率)

### 测试覆盖场景

✅ **正常流程**
- 获取策略列表（支持筛选）
- 获取策略详情（含完整代码）
- 创建新策略
- 更新策略
- 删除策略
- 验证代码
- 获取统计信息

✅ **错误处理**
- 策略不存在（404）
- 名称重复（409）
- 内置策略保护（403）

✅ **业务规则**
- 内置策略不允许修改代码
- 内置策略不允许删除
- 自动增加使用计数
- 代码验证集成

---

## 🔧 依赖关系

### Phase 2 依赖 Phase 1

Phase 2 的完整功能依赖于 Phase 1 的实现：

**Phase 1 待完成**:
1. ✅ 创建 `strategies` 数据库表
2. ✅ 提取内置策略代码到独立文件
3. ✅ 运行 `init_builtin_strategies.py` 初始化数据
4. ⚠️ 实现统一的 `DynamicCodeLoader`
5. ⚠️ 简化 `StrategyFactory`

**当前状态**:
- ✅ API 端点已实现
- ✅ 数据模型已定义
- ✅ Repository 已实现
- ⚠️ 策略加载功能待 Phase 1 完成后激活

---

## 📝 使用示例

### 1. 获取策略列表

```bash
curl -X GET "http://localhost:8000/api/strategies?source_type=builtin&page=1&page_size=20"
```

### 2. 获取策略详情（含完整代码）

```bash
curl -X GET "http://localhost:8000/api/strategies/1"
```

### 3. 验证策略代码

```bash
curl -X POST "http://localhost:8000/api/strategies/validate" \
  -H "Content-Type: application/json" \
  -d '{
    "code": "class MyStrategy(BaseStrategy):\n    def calculate_scores(self, prices, features, date):\n        return scores",
    "strict_mode": true
  }'
```

### 4. 创建策略

```bash
curl -X POST "http://localhost:8000/api/strategies" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "momentum_custom",
    "display_name": "自定义动量策略",
    "code": "...",
    "class_name": "CustomMomentumStrategy",
    "source_type": "custom",
    "category": "momentum",
    "tags": ["动量", "自定义"]
  }'
```

### 5. 运行回测（简化版）

```bash
curl -X POST "http://localhost:8000/api/backtest/run-v3" \
  -H "Content-Type: application/json" \
  -d '{
    "strategy_id": 1,
    "stock_pool": ["000001.SZ"],
    "start_date": "2023-01-01",
    "end_date": "2023-12-31",
    "initial_capital": 1000000.0
  }'
```

---

## 🚀 后续工作

### Phase 1 待完成

根据 [UNIFIED_DYNAMIC_STRATEGY_ARCHITECTURE.md](../docs/UNIFIED_DYNAMIC_STRATEGY_ARCHITECTURE.md) Phase 1：

1. **数据库迁移**
   - [ ] 创建 `strategies` 表（如果尚未创建）
   - [ ] 删除旧的 `strategy_configs` 表（可选，保持向后兼容）
   - [ ] 删除旧的 `ai_strategies` 表（可选，保持向后兼容）

2. **内置策略代码提取**
   - [ ] 创建 `core/scripts/builtin_strategies/` 目录
   - [ ] 提取动量策略完整代码
   - [ ] 提取均值回归策略完整代码
   - [ ] 提取多因子策略完整代码

3. **初始化脚本**
   - [ ] 编写 `core/scripts/init_builtin_strategies.py`
   - [ ] 运行初始化，将三个内置策略插入数据库

4. **统一加载器**
   - [ ] 简化 `DynamicCodeLoader`
   - [ ] 更新 `StrategyFactory`
   - [ ] 删除 `ConfigLoader`

### Phase 3: Frontend 重构

根据文档 Phase 3（3-4天）：

1. **类型定义更新**
   - [ ] 更新 `frontend/src/types/strategy.ts`
   - [ ] 删除旧类型定义

2. **策略管理页面**
   - [ ] 重写 `/strategies` 页面
   - [ ] 创建 `StrategyCard` 组件
   - [ ] 创建代码查看页面
   - [ ] 创建策略创建页面

3. **回测页面简化**
   - [ ] 移除三个 Tab
   - [ ] 改为单一策略选择器
   - [ ] 添加策略来源图标

4. **API 客户端更新**
   - [ ] 更新 `api-client.ts`
   - [ ] 添加新的 API 方法
   - [ ] 删除旧的 API 方法

---

## 🎯 总结

Phase 2 Backend API 重构已完成，实现了：

✅ **统一的数据模型** - 所有策略使用相同的 Schema
✅ **完整的数据访问层** - 灵活的 Repository 实现
✅ **RESTful API** - 9个统一的策略管理端点
✅ **简化的回测API** - 只需 strategy_id 一个参数
✅ **代码验证** - 集成 Core 的安全检查
✅ **使用统计** - 自动记录和更新指标
✅ **保护机制** - 内置策略不可修改/删除

**架构优势**:
- 📉 减少了 API 端点数量（从分散的多个端点 → 统一的9个端点）
- 📊 提升了代码透明度（所有策略代码完全可见）
- 🔧 简化了回测流程（移除 strategy_type 选择）
- 🗄️ 统一了数据存储（单一 strategies 表）

**下一步**: 等待 Phase 1 完成数据库迁移和统一加载器实现，即可完全激活新架构。

---

**作者**: Backend Team
**日期**: 2026-02-09
**版本**: 2.0.0
