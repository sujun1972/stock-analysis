# 策略与模型管理系统设计方案

**文档版本**: v1.0.0
**创建时间**: 2026-02-08
**目标**: 为 Web UI 提供统一的策略/模型列表，支持选择策略进行回测和预测

---

## 📋 目录

- [需求背景](#需求背景)
- [架构设计](#架构设计)
- [职责划分](#职责划分)
- [实施方案](#实施方案)
- [API 设计](#api-设计)
- [前端集成](#前端集成)
- [实施步骤](#实施步骤)

---

## 需求背景

### 用户期望的 Web UI 功能

在策略列表页面显示：

```
┌─────────────────────────────────────────────────────────┐
│              策略与模型列表                               │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  [ ] 5日均线上穿策略               [传统策略]  [回测]     │
│  [ ] 20日均线策略                  [传统策略]  [回测]     │
│  [ ] 复合指标策略                  [传统策略]  [回测]     │
│                                                          │
│  ─────────────────────────────────────────────────────  │
│                                                          │
│  [ ] 平安银行 LightGBM (2026-01-01) [ML模型]  [回测] [预测] │
│  [ ] 平安银行 GRU (2026-01-02)      [ML模型]  [回测] [预测] │
│  [ ] 招商银行 LightGBM (2026-01-03) [ML模型]  [回测] [预测] │
│                                                          │
└─────────────────────────────────────────────────────────┘

选中策略后可以：
✅ 回测（所有策略都支持）
✅ 预测（只有ML模型支持）
```

### 核心问题

1. **如何统一管理传统策略和ML模型？**
2. **如何自动发现已训练的ML模型？**
3. **如何区分策略的能力（回测 vs 预测）？**
4. **Core 和 Backend 的职责如何划分？**

---

## 架构设计

### 三层架构

```
┌─────────────────────────────────────────────────────────────┐
│                   Frontend（用户界面层）                     │
│   ┌───────────────────────────────────────────────────┐    │
│   │  策略列表组件 (StrategyList.tsx)                    │    │
│   │  ├─ 显示传统策略                                    │    │
│   │  ├─ 显示ML模型                                      │    │
│   │  ├─ 区分能力标签（回测/预测）                        │    │
│   │  └─ 触发回测/预测请求                                │    │
│   └───────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
                            ↓ HTTP API
┌─────────────────────────────────────────────────────────────┐
│                Backend（API服务层 + 资源管理层）              │
│   ┌───────────────────────────────────────────────────┐    │
│   │  新增 API 端点: /api/unified/list                  │    │
│   │  ├─ 调用 StrategyManager 获取传统策略               │    │
│   │  ├─ 调用 ModelRegistry 获取ML模型                  │    │
│   │  └─ 合并结果返回统一格式                            │    │
│   └───────────────────────────────────────────────────┘    │
│                                                              │
│   ┌───────────────────────────────────────────────────┐    │
│   │  策略管理器 (StrategyManager) ✅ 已存在             │    │
│   │  ├─ 管理传统策略（ComplexIndicatorStrategy等）      │    │
│   │  ├─ 提供策略元数据                                  │    │
│   │  └─ 策略实例化                                      │    │
│   └───────────────────────────────────────────────────┘    │
│                                                              │
│   ┌───────────────────────────────────────────────────┐    │
│   │  模型注册表 (ModelRegistry) ⭐ 需要新建              │    │
│   │  ├─ 扫描 data/models/ml_models/ 目录               │    │
│   │  ├─ 解析模型元数据 (*_meta.json)                   │    │
│   │  ├─ 提供模型列表、查询、过滤功能                     │    │
│   │  └─ 版本管理和最新模型获取                           │    │
│   └───────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
                            ↓ 调用
┌─────────────────────────────────────────────────────────────┐
│                    Core（核心业务逻辑层）                     │
│   ┌───────────────────────────────────────────────────┐    │
│   │  策略实现 (strategies/) ✅ 已存在                   │    │
│   │  ├─ ComplexIndicatorStrategy                       │    │
│   │  ├─ MLModelStrategy                                │    │
│   │  └─ 其他传统策略...                                 │    │
│   └───────────────────────────────────────────────────┘    │
│                                                              │
│   ┌───────────────────────────────────────────────────┐    │
│   │  ML模型训练/预测 (ml/, models/) ✅ 已存在           │    │
│   │  ├─ TrainedModel (封装训练好的模型)                 │    │
│   │  ├─ FeatureEngine (特征计算)                        │    │
│   │  └─ ModelTrainer (模型训练)                         │    │
│   └───────────────────────────────────────────────────┘    │
│                                                              │
│   ┌───────────────────────────────────────────────────┐    │
│   │  回测引擎 (backtest/) ✅ 已存在                     │    │
│   │  └─ BacktestEngine                                 │    │
│   └───────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
```

---

## 职责划分

### Core 项目（核心业务逻辑层）

**职责**：
- ✅ **实现策略逻辑**：具体的交易策略算法（如复合指标策略、ML策略）
- ✅ **实现ML模型**：模型训练、预测、特征工程
- ✅ **实现回测引擎**：执行回测、计算绩效
- ✅ **定义策略接口**：`BaseStrategy` 基类，定义元数据格式

**不需要做**：
- ❌ 不需要管理"哪些模型文件存在"
- ❌ 不需要提供 REST API
- ❌ 不需要知道 Web UI 的展示逻辑

**建议补充**（可选）：
```python
# core/src/strategies/base_strategy.py
class BaseStrategy:
    @classmethod
    def get_metadata(cls) -> Dict:
        """返回策略元数据"""
        return {
            "id": "...",
            "name": "...",
            "description": "...",
            "type": "traditional",  # 或 "ml"
            "supports_prediction": False,  # 传统策略不支持预测
            "supports_backtest": True,
        }
```

---

### Backend 项目（API服务层 + 资源管理层）⭐ 重点

**职责**：
- ✅ **资源发现**：扫描文件系统，发现已训练的模型文件
- ✅ **元数据管理**：解析模型元数据（`*_meta.json`），提供查询接口
- ✅ **统一接口**：将传统策略和ML模型合并成统一格式
- ✅ **API 端点**：提供 RESTful API 供前端调用
- ✅ **业务编排**：协调 Core 的功能，处理用户请求

**需要新增的模块**：

#### 1. 模型注册表服务 (ModelRegistry)

**文件位置**：`backend/app/services/model_registry.py`

**核心功能**：
```python
class ModelRegistry:
    """ML模型注册表"""

    def list_models(
        self,
        stock_code: Optional[str] = None,
        model_type: Optional[str] = None,
        limit: int = 100
    ) -> List[Dict[str, Any]]:
        """
        列出所有训练好的模型

        扫描逻辑:
        1. 遍历 data/models/ml_models/ 目录
        2. 查找所有 *_meta.json 文件
        3. 解析元数据，提取:
           - 股票代码 (symbol)
           - 模型类型 (model_type)
           - 训练时间 (created_at)
           - 性能指标 (metrics)
        4. 验证对应的模型文件是否存在 (.pkl, .pth)

        返回格式:
        [
            {
                "id": "000001_lightgbm_20260101_abc123",
                "name": "平安银行 LightGBM 模型 (2026-01-01)",
                "stock_code": "000001",
                "stock_name": "平安银行",
                "model_type": "lightgbm",
                "trained_at": "2026-01-01 14:30:00",
                "metrics": {
                    "ic": 0.08,
                    "rank_ic": 0.12,
                    "rmse": 0.02
                },
                "file_path": "data/models/ml_models/000001_lightgbm_xxx.pkl",
                "supports_prediction": true,
                "supports_backtest": true,
                "is_valid": true
            },
            ...
        ]
        """

    def get_model_by_id(self, model_id: str) -> Optional[Dict]:
        """根据模型ID获取详细信息"""

    def get_latest_model(
        self,
        stock_code: str,
        model_type: Optional[str] = None
    ) -> Optional[Dict]:
        """获取指定股票的最新模型"""
```

**实现要点**：
- 使用 `pathlib.Path` 扫描目录
- 使用 `json.load()` 解析元数据
- 按训练时间倒序排序
- 缓存模型列表（可选优化）

#### 2. 统一策略/模型 API 端点

**文件位置**：`backend/app/api/endpoints/unified.py`

**核心端点**：
```python
@router.get("/list")
async def list_strategies_and_models(
    type_filter: Optional[str] = None,  # 'traditional', 'ml', 或 None（全部）
    stock_code: Optional[str] = None,
    limit: int = 100
) -> Dict[str, Any]:
    """
    统一列出传统策略和ML模型

    返回格式:
    {
        "status": "success",
        "data": {
            "traditional_strategies": [
                {
                    "id": "complex_indicator",
                    "name": "复合指标策略",
                    "type": "traditional",
                    "supports_prediction": false,
                    "supports_backtest": true,
                    "parameter_count": 13
                },
                ...
            ],
            "ml_models": [
                {
                    "id": "000001_lightgbm_20260101_abc123",
                    "name": "平安银行 LightGBM 模型 (2026-01-01)",
                    "type": "ml",
                    "supports_prediction": true,
                    "supports_backtest": true,
                    "stock_code": "000001",
                    "trained_at": "2026-01-01 14:30:00",
                    "metrics": {...}
                },
                ...
            ],
            "total": 15
        }
    }
    """
    # 1. 获取传统策略
    traditional_strategies = []
    if type_filter in [None, 'traditional']:
        traditional_strategies = strategy_manager.list_strategies()
        # 添加统一字段
        for s in traditional_strategies:
            s['type'] = 'traditional'
            s['supports_prediction'] = False
            s['supports_backtest'] = True

    # 2. 获取ML模型
    ml_models = []
    if type_filter in [None, 'ml']:
        ml_models = model_registry.list_models(
            stock_code=stock_code,
            limit=limit
        )
        # 添加统一字段
        for m in ml_models:
            m['type'] = 'ml'
            m['supports_prediction'] = True
            m['supports_backtest'] = True

    return {
        "status": "success",
        "data": {
            "traditional_strategies": traditional_strategies,
            "ml_models": ml_models,
            "total": len(traditional_strategies) + len(ml_models)
        }
    }
```

---

## API 设计

### 新增 API 端点

#### 1. 统一列表 API

```http
GET /api/unified/list?type=all&stock_code=000001&limit=100

Response:
{
  "status": "success",
  "data": {
    "traditional_strategies": [...],
    "ml_models": [...],
    "total": 15
  }
}
```

#### 2. 模型注册表 API

```http
# 列出所有模型
GET /api/models/list?stock_code=000001&model_type=lightgbm&limit=50

# 获取模型详情
GET /api/models/{model_id}

# 获取最新模型
GET /api/models/latest?stock_code=000001&model_type=lightgbm
```

#### 3. 现有 API 保持不变

```http
# 策略元数据（已存在）
GET /api/strategy/list
GET /api/strategy/metadata?strategy_id=complex_indicator

# 回测（已存在）
POST /api/backtest/run

# 预测（已存在，只支持ML模型）
POST /api/ml/predict
```

---

## 前端集成

### 前端调用示例

```typescript
// 1. 获取统一列表
const response = await fetch('/api/unified/list?type=all');
const { data } = await response.json();

// 2. 渲染策略列表
const allItems = [
  ...data.traditional_strategies.map(s => ({
    ...s,
    category: '传统策略'
  })),
  ...data.ml_models.map(m => ({
    ...m,
    category: 'ML模型'
  }))
];

// 3. 根据 supports_prediction 显示按钮
{allItems.map(item => (
  <div key={item.id}>
    <h3>{item.name}</h3>
    <Badge>{item.category}</Badge>

    {/* 所有策略都支持回测 */}
    <Button onClick={() => runBacktest(item.id)}>
      回测
    </Button>

    {/* 只有ML模型支持预测 */}
    {item.supports_prediction && (
      <Button onClick={() => runPrediction(item.id)}>
        预测
      </Button>
    )}
  </div>
))}
```

---

## 实施步骤

### Phase 1: Backend 模型注册表 (2-3天)

#### Day 1: 实现 ModelRegistry 服务
- [ ] 创建 `backend/app/services/model_registry.py`
- [ ] 实现模型扫描逻辑
- [ ] 实现元数据解析
- [ ] 编写单元测试

**关键代码位置**：
```python
# backend/app/services/model_registry.py
class ModelRegistry:
    def __init__(self, model_dir: str = "data/models/ml_models"):
        self.model_dir = Path(model_dir)

    def list_models(self, stock_code=None, model_type=None, limit=100):
        # 扫描 *_meta.json
        # 解析元数据
        # 验证模型文件存在
        # 返回列表
```

#### Day 2: 实现统一 API 端点
- [ ] 创建 `backend/app/api/endpoints/unified.py`
- [ ] 实现 `/api/unified/list` 端点
- [ ] 合并传统策略和ML模型
- [ ] 编写集成测试

**关键代码位置**：
```python
# backend/app/api/endpoints/unified.py
@router.get("/list")
async def list_strategies_and_models():
    # 调用 strategy_manager.list_strategies()
    # 调用 model_registry.list_models()
    # 合并结果
```

#### Day 3: 注册路由 + 测试
- [ ] 在 `backend/app/main.py` 注册新路由
- [ ] 使用 Postman/curl 测试 API
- [ ] 编写 API 文档

```python
# backend/app/main.py
from app.api.endpoints import unified

app.include_router(
    unified.router,
    prefix="/api/unified",
    tags=["统一策略列表"]
)
```

---

### Phase 2: 前端集成 (2-3天)

#### Day 1: 创建策略列表组件
- [ ] 创建 `frontend/src/components/StrategyList.tsx`
- [ ] 调用 `/api/unified/list` API
- [ ] 渲染策略和模型

#### Day 2: 实现回测/预测按钮
- [ ] 区分显示"回测"和"预测"按钮
- [ ] 连接到现有回测/预测 API
- [ ] 处理加载状态和错误

#### Day 3: 优化 UI/UX
- [ ] 添加筛选功能（按类型、股票代码）
- [ ] 添加排序功能（按时间、性能）
- [ ] 添加分页

---

### Phase 3: Core 项目（可选优化）(1天)

#### 统一策略元数据格式
- [ ] 在 `core/src/strategies/base_strategy.py` 添加标准元数据接口
- [ ] 确保所有策略实现 `get_metadata()` 方法
- [ ] 添加 `supports_prediction` 和 `supports_backtest` 字段

```python
# core/src/strategies/base_strategy.py
class BaseStrategy:
    @classmethod
    def get_metadata(cls) -> Dict:
        return {
            "id": "...",
            "name": "...",
            "type": "traditional",  # 或 "ml"
            "supports_prediction": False,
            "supports_backtest": True,
        }
```

---

## 数据流示例

### 场景：用户在 Web UI 上查看策略列表

```
┌────────────────────────────────────────────────────────────┐
│ 1. 用户打开策略列表页面                                      │
└────────────────────────────────────────────────────────────┘
                      ↓
┌────────────────────────────────────────────────────────────┐
│ 2. 前端发起请求: GET /api/unified/list                      │
└────────────────────────────────────────────────────────────┘
                      ↓
┌────────────────────────────────────────────────────────────┐
│ 3. Backend 处理:                                            │
│    ├─ strategy_manager.list_strategies()                   │
│    │   → 返回 [复合指标策略, MA策略, ...]                    │
│    ├─ model_registry.list_models()                         │
│    │   → 扫描 data/models/ml_models/                       │
│    │   → 解析 *_meta.json                                  │
│    │   → 返回 [平安银行LGB模型, 招商银行GRU模型, ...]        │
│    └─ 合并并返回                                            │
└────────────────────────────────────────────────────────────┘
                      ↓
┌────────────────────────────────────────────────────────────┐
│ 4. 前端渲染:                                                │
│    ┌────────────────────────────────────────────────┐     │
│    │ [ ] 复合指标策略    [传统策略]  [回测]          │     │
│    │ [ ] MA策略          [传统策略]  [回测]          │     │
│    │ ─────────────────────────────────────────────  │     │
│    │ [ ] 平安银行 LGB   [ML模型]  [回测] [预测]     │     │
│    │ [ ] 招商银行 GRU   [ML模型]  [回测] [预测]     │     │
│    └────────────────────────────────────────────────┘     │
└────────────────────────────────────────────────────────────┘
```

---

## 关键设计原则

### 1. 单一职责原则
- **Core**: 只负责业务逻辑实现
- **Backend**: 负责资源管理和API服务
- **Frontend**: 负责UI展示和交互

### 2. 统一接口原则
所有策略和模型返回统一格式：
```json
{
  "id": "唯一标识",
  "name": "显示名称",
  "type": "traditional" | "ml",
  "supports_prediction": true/false,
  "supports_backtest": true/false,
  ...
}
```

### 3. 渐进式实现
- Phase 1: Backend 基础设施（不影响现有功能）
- Phase 2: 前端集成（可独立测试）
- Phase 3: Core 优化（可选）

---

## 常见问题 FAQ

### Q1: Core 需要做什么修改？
**A**: 几乎不需要修改！Core 已经提供了策略实现和ML模型，Backend 只是读取这些资源的元数据。

**可选优化**：在 `BaseStrategy` 添加 `get_metadata()` 方法，确保元数据格式统一。

---

### Q2: 如何区分传统策略和ML模型？
**A**: 在统一 API 中添加 `type` 字段：
- `type: "traditional"` → 传统策略（不支持预测）
- `type: "ml"` → ML模型（支持预测）

---

### Q3: 如何知道哪些模型文件存在？
**A**: Backend 的 `ModelRegistry` 会：
1. 扫描 `data/models/ml_models/` 目���
2. 查找所有 `*_meta.json` 文件
3. 验证对应的模型文件（`.pkl`, `.pth`）是否存在

---

### Q4: 训练新模型后需要手动注册吗？
**A**: 不需要！`ModelRegistry` 每次调用时会**动态扫描**目录，自动发现新模型。

为了性能，可以添加缓存（可选）：
```python
class ModelRegistry:
    def __init__(self):
        self._cache = None
        self._cache_time = None

    def list_models(self):
        # 缓存5分钟
        if self._cache and (time.time() - self._cache_time < 300):
            return self._cache

        # 重新扫描
        self._cache = self._scan_models()
        self._cache_time = time.time()
        return self._cache
```

---

### Q5: 预测功能只有ML模型支持，如何处理？
**A**: 在前端根据 `supports_prediction` 字段控制按钮显示：
```tsx
{item.supports_prediction && (
  <Button onClick={() => runPrediction(item.id)}>预测</Button>
)}
```

后端 `/api/ml/predict` 端点已经只接受 ML 模型 ID。

---

## 附录：文件结构变化

### Backend 新增文件

```
backend/
├── app/
│   ├── services/
│   │   └── model_registry.py          ⭐ 新增：模型注册表
│   └── api/
│       └── endpoints/
│           └── unified.py              ⭐ 新增：统一API端点
│
├── tests/
│   └── unit/
│       └── services/
│           └── test_model_registry.py  ⭐ 新增：测试
│
└── docs/
    └── planning/
        └── strategy_and_model_management.md  ⭐ 本文档
```

### Core 项目（无需修改，可选优化）

```
core/
└── src/
    └── strategies/
        └── base_strategy.py  # 可选：添加 get_metadata() 方法
```

---

## 总结

### 你需要做什么？

1. **Core 项目**：✅ 几乎不需要改动（可选优化元数据接口）

2. **Backend 项目**：⭐ 核心工作
   - 实现 `ModelRegistry` 服务（扫描模型文件）
   - 实现 `/api/unified/list` 端点（合并策略和模型）
   - 编写测试

3. **Frontend 项目**：
   - 调用新 API
   - 渲染统一列表
   - 根据 `supports_prediction` 显示按钮

### 架构优势

- ✅ **职责清晰**：Core 专注业务逻辑，Backend 管理资源
- ✅ **自动发现**：训练新模型后自动出现在列表中
- ✅ **统一接口**：前端只需调用一个 API
- ✅ **易于扩展**：未来添加新策略类型很容易

---

**文档版本**: v1.0.0
**创建时间**: 2026-02-08
**下一步**: 开始实施 Phase 1 - Backend 模型注册表
