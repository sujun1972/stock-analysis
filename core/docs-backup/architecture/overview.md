# 架构总览详解

**Stock-Analysis Core Architecture Overview**

**版本**: v3.0.0
**最后更新**: 2026-02-06

---

## 📐 架构设计理念

### 核心原则

Stock-Analysis Core 的架构设计遵循以下核心原则：

1. **分层架构 (Layered Architecture)**
   - 清晰的职责划分
   - 单向依赖流
   - 易于测试和维护

2. **模块化设计 (Modular Design)**
   - 高内聚、低耦合
   - 可插拔组件
   - 独立部署能力

3. **领域驱动 (Domain-Driven)**
   - 业务逻辑与技术实现分离
   - 清晰的领域边界
   - 统一语言 (Ubiquitous Language)

4. **可扩展性 (Extensibility)**
   - 策略模式 (Strategy Pattern)
   - 工厂模式 (Factory Pattern)
   - 插件化架构

---

## 🏛️ 六层架构

### 架构全景图

```
┌─────────────────────────────────────────────────────────────────┐
│                      🖥️  CLI / API Layer                         │
│                         (用户接口层)                              │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐             │
│  │ stock-cli   │  │  FastAPI    │  │  Jupyter    │             │
│  │  Commands   │  │  Endpoints  │  │  Notebooks  │             │
│  └─────────────┘  └─────────────┘  └─────────────┘             │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                    📋 Application Layer                          │
│                         (应用层)                                 │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │  Backtest    │  │   Analysis   │  │   Training   │          │
│  │  Orchestrator│  │   Service    │  │   Pipeline   │          │
│  └──────────────┘  └──────────────┘  └──────────────┘          │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                     💼 Business Layer                            │
│                         (业务层)                                 │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │  Strategies  │  │    Models    │  │  Risk Mgmt   │          │
│  │  (5 types)   │  │  (3 types)   │  │  (VaR/CVaR)  │          │
│  └──────────────┘  └──────────────┘  └──────────────┘          │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                     🧬 Feature Layer                             │
│                         (特征层)                                 │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │ Alpha Factors│  │  Indicators  │  │Feature Store │          │
│  │   (125+)     │  │    (60+)     │  │  (Cache)     │          │
│  └──────────────┘  └──────────────┘  └──────────────┘          │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                      💾 Data Layer                               │
│                         (数据层)                                 │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │   Providers  │  │   Storage    │  │   Quality    │          │
│  │ (AK/Tushare) │  │(TimescaleDB) │  │ (Validators) │          │
│  └──────────────┘  └──────────────┘  └──────────────┘          │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                  🔧 Infrastructure Layer                         │
│                       (基础设施层)                               │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │  TimescaleDB │  │     Redis    │  │    Loguru    │          │
│  │   (Storage)  │  │    (Cache)   │  │   (Logging)  │          │
│  └──────────────┘  └──────────────┘  └──────────────┘          │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │  Prometheus  │  │    Docker    │  │    Pytest    │          │
│  │ (Monitoring) │  │ (Container)  │  │   (Testing)  │          │
│  └──────────────┘  └──────────────┘  └──────────────┘          │
└─────────────────────────────────────────────────────────────────┘
```

---

## 📁 目录结构

### 项目结构概览

```
src/
├── data/                    # 数据层
│   ├── providers/           # 数据源适配器
│   │   ├── akshare_provider.py
│   │   └── tushare_provider.py
│   ├── database_manager.py  # 数据库管理
│   └── data_validator.py    # 数据质量检查
│
├── features/                # 特征层
│   ├── technical_indicators.py    # 60+ 技术指标
│   ├── alpha_factors/              # 125+ Alpha因子
│   │   ├── momentum_factors.py     # 动量因子
│   │   ├── volatility_factors.py   # 波动率因子
│   │   ├── reversal_factors.py     # 反转因子
│   │   └── ... (8个模块)
│   └── feature_store.py            # 特征存储
│
├── models/                  # 模型层
│   ├── lightgbm_model.py    # LightGBM
│   ├── gru_model.py         # GRU深度学习
│   ├── ridge_model.py       # Ridge回归
│   ├── model_trainer.py     # 训练器
│   └── ensemble.py          # 模型集成
│
├── strategies/              # 策略层
│   ├── base_strategy.py     # 策略基类
│   ├── alpha_strategy.py    # Alpha策略
│   ├── mean_reversion.py    # 均值回归
│   ├── three_layer/         # 三层架构（v3.0新增）⭐
│   │   ├── base.py          # 三层基类
│   │   ├── selectors/       # 选股器层
│   │   │   ├── momentum_selector.py
│   │   │   ├── reversal_selector.py
│   │   │   ├── ml_selector.py       # 机器学习选股⭐
│   │   │   └── external_selector.py
│   │   ├── entries/         # 入场策略层
│   │   │   ├── immediate_entry.py
│   │   │   ├── ma_breakout_entry.py
│   │   │   └── rsi_oversold_entry.py
│   │   └── exits/           # 退出策略层
│   │       ├── fixed_period_exit.py
│   │       ├── stop_loss_exit.py
│   │       ├── atr_stop_exit.py
│   │       └── trend_exit.py
│   └── ... (5种经典策略)
│
├── backtest/                # 回测层
│   ├── backtest_engine.py   # 回测引擎
│   ├── performance_metrics.py  # 性能指标
│   └── parallel_backtest.py    # 并行回测
│
└── utils/                   # 工具层
    ├── logger.py            # 日志
    ├── exceptions.py        # 异常
    └── response.py          # 统一响应
```

---

## 🎯 三层策略架构（v3.0 核心升级）

### 架构设计理念

三层架构是 v3.0 的核心创新，将传统的"策略"概念解耦为三个独立层级：

```
┌─────────────────────────────────────────────────────────────────┐
│  Layer 1: StockSelector (选股器层)                               │
│  职责: 从全市场筛选候选股票池                                      │
│  频率: 周频/月频                                                  │
│  输出: List[股票代码]                                             │
└─────────────────┬───────────────────────────────────────────────┘
                  ↓
┌─────────────────────────────────────────────────────────────────┐
│  Layer 2: EntryStrategy (入场策略层)                             │
│  职责: 决定何时买入候选股票                                        │
│  频率: 日频                                                       │
│  输出: Dict[股票代码, 买入信号]                                    │
└─────────────────┬───────────────────────────────────────────────┘
                  ↓
┌─────────────────────────────────────────────────────────────────┐
│  Layer 3: ExitStrategy (退出策略层)                              │
│  职责: 管理持仓，决定何时卖出                                      │
│  频率: 日频/实时                                                  │
│  输出: Dict[股票代码, 卖出信号]                                    │
└─────────────────────────────────────────────────────────────────┘
```

### 三层架构组件清单

#### 1. 选股器层 (StockSelector)

| 选股器 | 描述 | 核心参数 |
|--------|------|---------|
| **MomentumSelector** | 动量选股 | `lookback_period`, `top_n` |
| **ReversalSelector** | 反转选股 | `lookback_period`, `top_n` |
| **MLSelector** ⭐ | 机器学习选股 | `mode`, `features`, `top_n` |
| **ExternalSelector** | 外部系统集成 | `source`, `config` |

**MLSelector 核心特性**（v3.0 重点）：
- ✅ **多因子加权模式**: 支持 4 种归一化方法（z_score、min_max、rank、none）
- ✅ **LightGBM 排序模式**: 5 档智能评分，NDCG@10 优化
- ✅ **因子库集成**: 125+ Alpha 因子 + 60+ 技术指标
- ✅ **通配符特征**: 支持 `alpha:*`、`tech:rsi` 等表达式

#### 2. 入场策略层 (EntryStrategy)

| 入场策略 | 描述 | 触发条件 |
|---------|------|---------|
| **ImmediateEntry** | 立即入场 | 选中即买入 |
| **MABreakoutEntry** | 均线突破 | 价格突破 MA20 |
| **RSIOversoldEntry** | RSI 超卖 | RSI < 30 |

#### 3. 退出策略层 (ExitStrategy)

| 退出策略 | 描述 | 触发条件 |
|---------|------|---------|
| **FixedPeriodExit** | 固定持有期 | 持有 N 天后卖出 |
| **FixedStopLossExit** | 固定止损 | 亏损达到 X% |
| **ATRStopLossExit** | ATR 动态止损 | 亏损超过 N 倍 ATR |
| **TrendExitStrategy** | 趋势退出 | MA5 下穿 MA20 |

### 组合示例

三层架构支持 **3×3×4 = 36+ 种策略组合**：

```python
from src.strategies.three_layer import (
    MLSelector,
    ImmediateEntry,
    FixedStopLossExit,
    StrategyComposer
)

# 组合 1: ML 选股 + 立即入场 + 固定止损
composer = StrategyComposer(
    selector=MLSelector(params={
        'mode': 'lightgbm_ranker',
        'model_path': './models/stock_ranker.pkl',
        'top_n': 50
    }),
    entry=ImmediateEntry(),
    exit_strategy=FixedStopLossExit(params={'stop_loss_pct': -5.0}),
    rebalance_freq='W'  # 周度调仓
)

# 组合 2: 动量选股 + MA 突破 + 趋势退出
composer = StrategyComposer(
    selector=MomentumSelector(params={'lookback_period': 20, 'top_n': 50}),
    entry=MABreakoutEntry(params={'ma_window': 20}),
    exit_strategy=TrendExitStrategy(params={'fast': 5, 'slow': 20}),
    rebalance_freq='M'  # 月度调仓
)
```

### 架构优势

1. **高度解耦**: 选股、入场、退出逻辑独立开发和测试
2. **灵活组合**: 3 个选股器 × 3 个入场策略 × 4 个退出策略 = 36+ 种组合
3. **易于扩展**: 新增策略只需实现对应层的接口
4. **频率独立**: 选股周频，入场/退出日频，互不干扰
5. **向后兼容**: 保留原有 BaseStrategy 接口

---

## 🔄 数据流与控制流

### 1. 完整交易工作流

```
┌─────────────┐
│ 用户请求     │ (CLI/API)
└──────┬──────┘
       ↓
┌─────────────────────────────────────────┐
│ Application Layer: 编排业务流程          │
│ - 参数验证                               │
│ - 任务分解                               │
│ - 结果聚合                               │
└──────┬──────────────────────────────────┘
       ↓
┌─────────────────────────────────────────┐
│ Step 1: 数据获取 (Data Layer)            │
│ ┌─────────────┐                         │
│ │ Providers   │ → 获取原始行情数据       │
│ └─────────────┘                         │
│ ┌─────────────┐                         │
│ │ Validators  │ → 数据质量检查           │
│ └─────────────┘                         │
│ ┌─────────────┐                         │
│ │ Database    │ → 存储到TimescaleDB      │
│ └─────────────┘                         │
└──────┬──────────────────────────────────┘
       ↓
┌─────────────────────────────────────────┐
│ Step 2: 特征计算 (Feature Layer)         │
│ ┌─────────────┐                         │
│ │ Indicators  │ → 计算技术指标 (60+)     │
│ └─────────────┘                         │
│ ┌─────────────┐                         │
│ │Alpha Factors│ → 计算Alpha因子 (125+)   │
│ └─────────────┘                         │
│ ┌─────────────┐                         │
│ │Feature Cache│ → 缓存特征结果           │
│ └─────────────┘                         │
└──────┬──────────────────────────────────┘
       ↓
┌─────────────────────────────────────────┐
│ Step 3: 模型预测 (Business Layer)        │
│ ┌─────────────┐                         │
│ │ Models      │ → 机器学习预测           │
│ │(LGB/GRU/...)│   (3种模型可选)          │
│ └─────────────┘                         │
│ ┌─────────────┐                         │
│ │ Ensemble    │ → 模型集成               │
│ └─────────────┘                         │
└──────┬──────────────────────────────────┘
       ↓
┌─────────────────────────────────────────┐
│ Step 4: 策略决策 (Business Layer)        │
│ ┌─────────────┐                         │
│ │ Strategies  │ → 生成交易信号           │
│ │(5种经典策略)│   (买入/卖出/持有)       │
│ └─────────────┘                         │
│ ┌─────────────────────────────────────┐ │
│ │ 三层架构 (v3.0)                      │ │
│ │ Selector → Entry → Exit              │ │
│ │ (36+ 种组合)                         │ │
│ └─────────────────────────────────────┘ │
│ ┌─────────────┐                         │
│ │Risk Manager │ → 风控检查               │
│ └─────────────┘                         │
└──────┬──────────────────────────────────┘
       ↓
┌─────────────────────────────────────────┐
│ Step 5: 回测验证 (Application Layer)     │
│ ┌─────────────┐                         │
│ │Backtest Eng │ → 历史数据验证           │
│ └─────────────┘                         │
│ ┌─────────────┐                         │
│ │Performance  │ → 计算绩效指标 (30+)     │
│ └─────────────┘                         │
└──────┬──────────────────────────────────┘
       ↓
┌─────────────┐
│ 结果返回     │ (Response对象)
└─────────────┘
```

---

## 🎯 关键设计模式

### 1. 策略模式 (Strategy Pattern)

**应用场景**: 交易策略、数据源切换

```python
# 策略基类
class BaseStrategy(ABC):
    @abstractmethod
    def generate_signals(self, data: pd.DataFrame) -> pd.Series:
        pass

# 具体策略
class AlphaStrategy(BaseStrategy):
    def generate_signals(self, data: pd.DataFrame) -> pd.Series:
        # 实现具体策略逻辑
        pass

# 策略使用
strategy = AlphaStrategy()
signals = strategy.generate_signals(data)
```

### 2. 工厂模式 (Factory Pattern)

**应用场景**: 模型创建、数据源创建

```python
class ModelFactory:
    @staticmethod
    def create_model(model_type: str) -> BaseModel:
        if model_type == "lightgbm":
            return LightGBMModel()
        elif model_type == "gru":
            return GRUModel()
        # ...
```

### 3. 单例模式 (Singleton Pattern)

**应用场景**: 数据库连接、配置管理

```python
class DatabaseManager:
    _instance = None

    def __new__(cls):
        if cls._instance is None:
            cls._instance = super().__new__(cls)
        return cls._instance
```

### 4. 装饰器模式 (Decorator Pattern)

**应用场景**: 异常处理、性能监控、缓存

```python
@handle_exceptions
@time_it
@cache_result
def compute_alpha_factor(data: pd.DataFrame) -> pd.Series:
    # 计算因子逻辑
    pass
```

---

## 🔐 依赖管理

### 依赖方向规则

```
CLI/API Layer
    ↓ (可以依赖)
Application Layer
    ↓ (可以依赖)
Business Layer
    ↓ (可以依赖)
Feature Layer
    ↓ (可以依赖)
Data Layer
    ↓ (可以依赖)
Infrastructure Layer

❌ 禁止反向依赖
✅ 允许同层交互（通过接口）
```

### 依赖注入 (Dependency Injection)

```python
class BacktestEngine:
    def __init__(
        self,
        data_provider: DataProvider,  # 注入依赖
        strategy: BaseStrategy,        # 注入依赖
        risk_manager: RiskManager      # 注入依赖
    ):
        self.data_provider = data_provider
        self.strategy = strategy
        self.risk_manager = risk_manager
```

---

## 📊 架构优势

### 1. 可测试性 (Testability)

- ✅ 每层独立测试
- ✅ Mock外部依赖
- ✅ 90%+测试覆盖率

### 2. 可维护性 (Maintainability)

- ✅ 清晰的模块边界
- ✅ 单一职责原则
- ✅ 代码平均<500行/文件

### 3. 可扩展性 (Extensibility)

- ✅ 插件化策略
- ✅ 多数据源支持
- ✅ 新模型快速集成

### 4. 性能 (Performance)

- ✅ 向量化计算 (35x加速)
- ✅ 并行回测 (3-8x加速)
- ✅ LRU缓存机制

---

## 🔄 架构演进

### v1.0.0 → v2.0.0

- ✅ 引入策略层
- ✅ 添加风险管理
- ✅ 因子分析工具

### v2.0.0 → v3.0.0

- ✅ API标准化 (Response格式)
- ✅ 统一异常系统
- ✅ 模块化重构

### 未来规划 (v4.0.0)

- 📋 微服务化改造
- 📋 分布式计算支持
- 📋 实盘交易系统

---

## 📊 核心指标

| 指标 | 数值 | 说明 |
|------|------|------|
| 代码规模 | 50,000+行 | 核心代码 + 12,000行三层架构 |
| 测试覆盖率 | 90%+ | 3,200+测试用例 |
| 三层架构测试 | 385用例 | 100% 通过 |
| MLSelector测试 | 120+用例 | 100% 通过 |
| API一致性 | 95%+ | 统一Response+异常 |
| 架构评分 | 4.9/5.0 | 生产就绪 |
| 因子数量 | 125+ Alpha + 60+ 技术指标 | 185+ 总计 |
| 策略组合 | 36+ 种 | 三层架构灵活组合 |

---

## 📚 相关文档

- 🎨 [设计模式详解](design_patterns.md) - 10种设计模式应用
- ⚡ [性能优化分析](performance.md) - 35倍性能提升
- 🔧 [技术栈详解](tech_stack.md) - 完整技术选型
- 🗺️ [开发路线图](../ROADMAP.md) - 项目规划

---

**文档版本**: v3.0.0
**维护团队**: Quant Team
**最后更新**: 2026-02-06
**v3.0 核心升级**: 三层策略架构 + MLSelector 机器学习选股
