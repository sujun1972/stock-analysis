# Example environment configuration for the stock-analysis project
# Copy this file to .env and fill in real values; .env itself is ignored by git.

# ==================== 应用配置 ====================
APP_ENVIRONMENT=development
APP_DEBUG=true
APP_LOG_LEVEL=INFO

# ==================== 数据库配置 ====================
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_DATABASE=stock_analysis
DATABASE_USER=stock_user
DATABASE_PASSWORD=your_password_here

# ==================== 数据源配置 ====================
# 主要数据源: 'akshare'(推荐，免费), 'tushare'(需要Token)
DATA_PROVIDER=akshare

# Tushare Pro API Token (可选，仅使用Tushare时需要)
# 在 https://tushare.pro/ 注册获取
DATA_TUSHARE_TOKEN=your_tushare_token_here

# DeepSeek AI API Key (用于AI分析)
DATA_DEEPSEEK_API_KEY=your_deepseek_api_key_here

# ==================== 路径配置 ====================
PATH_DATA_DIR=/data
PATH_MODELS_DIR=/data/models/ml_models
PATH_CACHE_DIR=/data/pipeline_cache
PATH_RESULTS_DIR=/data/backtest_results

# ==================== 机器学习配置 ====================
ML_DEFAULT_TARGET_PERIOD=5
ML_DEFAULT_SCALER_TYPE=robust
ML_DEFAULT_TRAIN_RATIO=0.7
ML_DEFAULT_VALID_RATIO=0.15
ML_CACHE_FEATURES=true
ML_FEATURE_VERSION=v2.0

# ==================== 向后兼容 (旧版本使用) ====================
# 保留以兼容尚未迁移的代码
DEEPSEEK_API_KEY=${DATA_DEEPSEEK_API_KEY}
TUSHARE_TOKEN=${DATA_TUSHARE_TOKEN}
