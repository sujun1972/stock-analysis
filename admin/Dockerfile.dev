# Admin 开发模式 Dockerfile
FROM node:18-alpine AS development

WORKDIR /app

# 安装依赖
COPY package.json package-lock.json* ./
RUN npm ci

# 复制配置文件
COPY next.config.mjs ./
COPY tsconfig.json ./
COPY tailwind.config.ts ./
COPY postcss.config.mjs ./

# 暴露端口
EXPOSE 3002

# 开发模式启动命令（支持热重载）
CMD ["npm", "run", "dev"]
